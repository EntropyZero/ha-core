import time


class AnomalyDetector:
    def __init__(self, config):
        self.config = config

    def detect(self, batch_data):
        # Run the model Isabel!!!
        time.sleep(10)
        # Randomly return True or False based on random number
        return "FLOOOOOOOOOOOOOOOOOD" if time.time() % 2 == 0 else "Normal"

    def train(self, training_data):
        # Train the model Isabel!!!
        time.sleep(60)
        return True
